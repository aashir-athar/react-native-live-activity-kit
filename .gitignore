node_modules/
.yarn/cache
.yarn/install-state.gz
.pnp.*

# JS build output (react-native-builder-bob, plugin tsc)
lib/
plugin/build/
*.tsbuildinfo

# NOTE: nitrogen/generated/ is intentionally committed so the native build works
# without consumers running codegen. Regenerate with `npm run codegen`.

*.log
npm-debug.log*
yarn-error.log*
.npm/

*.tgz

# Secrets — NEVER commit (APNs auth keys especially)
.env
.env.*
!.env.example
*.p8
*.p12
*.pem
*.keystore
*.jks
keystore.properties
google-services.json
GoogleService-Info.plist

# Editors & OS
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# Android
android/.gradle/
android/build/
android/.cxx/
*.apk
*.aab
local.properties

# iOS / CocoaPods
ios/build/
ios/Pods/
*.xcworkspace
xcuserdata/

# Example app
example/node_modules/
example/.expo/
example/android/
example/ios/
example/.gradle/
