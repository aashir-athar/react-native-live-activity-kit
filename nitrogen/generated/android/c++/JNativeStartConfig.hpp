///
/// JNativeStartConfig.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "NativeStartConfig.hpp"

#include "JNativeLiveActivityAttributes.hpp"
#include "JNativeLiveActivityState.hpp"
#include "NativeLiveActivityAttributes.hpp"
#include "NativeLiveActivityState.hpp"
#include <optional>
#include <string>
#include <unordered_map>

namespace margelo::nitro::liveactivitykit {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "NativeStartConfig" and the the Kotlin data class "NativeStartConfig".
   */
  struct JNativeStartConfig final: public jni::JavaClass<JNativeStartConfig> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/liveactivitykit/NativeStartConfig;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct NativeStartConfig by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    NativeStartConfig toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldAttributes = clazz->getField<JNativeLiveActivityAttributes>("attributes");
      jni::local_ref<JNativeLiveActivityAttributes> attributes = this->getFieldValue(fieldAttributes);
      static const auto fieldState = clazz->getField<JNativeLiveActivityState>("state");
      jni::local_ref<JNativeLiveActivityState> state = this->getFieldValue(fieldState);
      static const auto fieldStaleDateMs = clazz->getField<jni::JDouble>("staleDateMs");
      jni::local_ref<jni::JDouble> staleDateMs = this->getFieldValue(fieldStaleDateMs);
      static const auto fieldRelevanceScore = clazz->getField<jni::JDouble>("relevanceScore");
      jni::local_ref<jni::JDouble> relevanceScore = this->getFieldValue(fieldRelevanceScore);
      return NativeStartConfig(
        attributes->toCpp(),
        state->toCpp(),
        staleDateMs != nullptr ? std::make_optional(staleDateMs->value()) : std::nullopt,
        relevanceScore != nullptr ? std::make_optional(relevanceScore->value()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JNativeStartConfig::javaobject> fromCpp(const NativeStartConfig& value) {
      using JSignature = JNativeStartConfig(jni::alias_ref<JNativeLiveActivityAttributes>, jni::alias_ref<JNativeLiveActivityState>, jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JDouble>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        JNativeLiveActivityAttributes::fromCpp(value.attributes),
        JNativeLiveActivityState::fromCpp(value.state),
        value.staleDateMs.has_value() ? jni::JDouble::valueOf(value.staleDateMs.value()) : nullptr,
        value.relevanceScore.has_value() ? jni::JDouble::valueOf(value.relevanceScore.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::liveactivitykit
