///
/// JNativeUpdateConfig.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "NativeUpdateConfig.hpp"

#include "JNativeLiveActivityState.hpp"
#include "NativeLiveActivityState.hpp"
#include <optional>
#include <string>
#include <unordered_map>

namespace margelo::nitro::liveactivitykit {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "NativeUpdateConfig" and the the Kotlin data class "NativeUpdateConfig".
   */
  struct JNativeUpdateConfig final: public jni::JavaClass<JNativeUpdateConfig> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/liveactivitykit/NativeUpdateConfig;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct NativeUpdateConfig by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    NativeUpdateConfig toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldState = clazz->getField<JNativeLiveActivityState>("state");
      jni::local_ref<JNativeLiveActivityState> state = this->getFieldValue(fieldState);
      static const auto fieldStaleDateMs = clazz->getField<jni::JDouble>("staleDateMs");
      jni::local_ref<jni::JDouble> staleDateMs = this->getFieldValue(fieldStaleDateMs);
      static const auto fieldRelevanceScore = clazz->getField<jni::JDouble>("relevanceScore");
      jni::local_ref<jni::JDouble> relevanceScore = this->getFieldValue(fieldRelevanceScore);
      static const auto fieldAlertTitle = clazz->getField<jni::JString>("alertTitle");
      jni::local_ref<jni::JString> alertTitle = this->getFieldValue(fieldAlertTitle);
      static const auto fieldAlertBody = clazz->getField<jni::JString>("alertBody");
      jni::local_ref<jni::JString> alertBody = this->getFieldValue(fieldAlertBody);
      static const auto fieldAlertSound = clazz->getField<jni::JString>("alertSound");
      jni::local_ref<jni::JString> alertSound = this->getFieldValue(fieldAlertSound);
      return NativeUpdateConfig(
        state->toCpp(),
        staleDateMs != nullptr ? std::make_optional(staleDateMs->value()) : std::nullopt,
        relevanceScore != nullptr ? std::make_optional(relevanceScore->value()) : std::nullopt,
        alertTitle != nullptr ? std::make_optional(alertTitle->toStdString()) : std::nullopt,
        alertBody != nullptr ? std::make_optional(alertBody->toStdString()) : std::nullopt,
        alertSound != nullptr ? std::make_optional(alertSound->toStdString()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JNativeUpdateConfig::javaobject> fromCpp(const NativeUpdateConfig& value) {
      using JSignature = JNativeUpdateConfig(jni::alias_ref<JNativeLiveActivityState>, jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JString>, jni::alias_ref<jni::JString>, jni::alias_ref<jni::JString>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        JNativeLiveActivityState::fromCpp(value.state),
        value.staleDateMs.has_value() ? jni::JDouble::valueOf(value.staleDateMs.value()) : nullptr,
        value.relevanceScore.has_value() ? jni::JDouble::valueOf(value.relevanceScore.value()) : nullptr,
        value.alertTitle.has_value() ? jni::make_jstring(value.alertTitle.value()) : nullptr,
        value.alertBody.has_value() ? jni::make_jstring(value.alertBody.value()) : nullptr,
        value.alertSound.has_value() ? jni::make_jstring(value.alertSound.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::liveactivitykit
